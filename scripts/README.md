# Scripts

Use this directory for repeatable data and reporting tasks.

Suggested scripts:

- `normalize_sources.*`: clean and normalize raw tool evidence
- `score_rollup.*`: aggregate audit scores into summary tables
- `export_report.*`: generate markdown/html outputs
